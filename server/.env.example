PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=*
# Optional production database connection; MVP defaults to in-memory demo data.
DATABASE_URL=
# Add payment provider keys only in server environment; never in the mobile app.
PAYMENT_PROVIDER=mock
