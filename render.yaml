services:
  - type: web
    name: rideon-api
    env: docker
    rootDir: server
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PAYMENT_PROVIDER
        value: paytm
